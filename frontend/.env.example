# Environment Configuration for Fractional

# Stellar Network Configuration
# Use 'testnet' or 'mainnet'
VITE_STELLAR_NETWORK=testnet

# Stellar Horizon Server URLs
VITE_STELLAR_HORIZON_URL=https://horizon-testnet.stellar.org
VITE_STELLAR_SOROBAN_RPC_URL=https://soroban-testnet.stellar.org

# Contract IDs (Deploy your contract and add the ID here)
VITE_LAND_REGISTRY_CONTRACT_ID=your_contract_id_here

# Network Passphrase
VITE_STELLAR_NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# Document Processing
# Documents are hashed locally using SHA-256 (no external IPFS service needed)
# Actual document verification is done off-chain by the Land Inspector

# Application Configuration
VITE_APP_NAME=Fractional
VITE_APP_VERSION=1.0.0
